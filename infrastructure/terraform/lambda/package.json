{
  "name": "apnidukaan-image-optimizer",
  "version": "1.0.0",
  "description": "Lambda function for optimizing and resizing product images",
  "main": "index.js",
  "scripts": {
    "build": "npm install --production && zip -r image_optimizer.zip ."
  },
  "dependencies": {
    "aws-sdk": "^2.1490.0",
    "sharp": "^0.32.6",
    "uuid": "^9.0.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
